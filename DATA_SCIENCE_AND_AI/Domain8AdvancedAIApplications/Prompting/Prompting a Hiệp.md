```
tôi chưa biết là tôi cần phải làm gì nữa khi cầm dự án này. Các hạ tầng và template có sẵn rồi. Lên reasoning cho tôi 5 cái đi. Context:   Mỗi agents sẽ cần có một prompt chất lượng và cần đáp ứng nhiều tiêu chí thành công khác nhau (cả về trải nghiệm và kĩ thuật). bối cảnh là dự án làm tối ưu từng prompt một cho agents. Sử dụng một platform được input vào 10-20 phiên bản prompt có cấu trúc khác nhau để kiểm thử và chọn được ra prompt có kết quả perform tốt nhất. Phần một: Mô tả hệ thống testing prompt cho agents Hệ thống này cho phép mô phỏng và đánh giá chất lượng prompt của các agent thông qua các “cuộc hội thoại giả lập” giữa: * Agent: mô hình/ứng dụng bạn muốn kiểm thử. * Persona: một mô hình khác được cấu hình để “đóng vai người dùng” với bối cảnh, mục tiêu và phong cách tương tác cụ thể. Mỗi lần chạy mô phỏng sẽ tạo ra một phiên hội thoại giữa một cặp Agent – Persona và kết quả được chấm điểm tự động dựa trên các tiêu chí đã cấu hình trước. Các thành phần chính 1. Persona (Add Persona) * Định nghĩa chân dung “người dùng ảo”: * Bối cảnh (context) * Mục tiêu (goals) * Hành vi / phong cách hỏi đáp * Có thể thêm nhiều persona để test cùng một agent trong nhiều kịch bản khác nhau (user khó tính, user gà mờ, user kỹ thuật, v.v.). 2. Cấu hình chạy mô phỏng (Runs per Pair & Simulations) * Runs per Pair: số lần chạy mô phỏng cho mỗi cặp Agent – Persona (tăng số lần để có thống kê ổn định hơn). * Start Simulation: nút khởi chạy toàn bộ mô phỏng theo cấu hình hiện tại. * Simulations / Total runs: hiển thị số lượng lượt chạy đã hoặc sẽ được thực thi. * Khi chưa có lần chạy nào, hệ thống hiển thị trạng thái như: * Total: 0 simulations * Report: No simulations found. 3. Evaluation Config (Cấu hình đánh giá) a. Evaluator Model * Mô hình dùng để tự động chấm điểm và nhận xét chất lượng câu trả lời của agent. * Thay vì con người review từng hội thoại, evaluator model đọc lại log hội thoại và đánh giá theo tiêu chí bạn đưa ra. b. Criteria * Tập các tiêu chí đánh giá (criteria), ví dụ: * Đúng nội dung / chính xác thông tin * Hợp lý / logic * Đủ bước / đầy đủ * An toàn / tuân thủ policy * Thân thiện / dễ hiểu cho người dùng * Có thể thêm / chỉnh sửa tiêu chí tuỳ use case bằng nút Add. c. Pass Score * Ngưỡng điểm để xem một lượt mô phỏng là “pass” hay “fail”. * Có thể thiết lập: * Pass score cho từng tiêu chí (VD: ≥ 0.7 cho “Correctness”) * Hoặc pass score tổng hợp cho toàn bộ lượt chạy. Báo cáo (Report) * Sau khi mô phỏng xong, hệ thống sinh report cho từng cặp Agent – Persona và từng tiêu chí: * Điểm số từng lần chạy * Tỉ lệ pass/fail * Nhận xét chi tiết (nếu evaluator model hỗ trợ giải thích) Phần 2: Các agents có thể như nào:    Agent Sở Thích (Interest Agent) * Agent Toy * Agent Music * Agent Movie * Agent Cartoon Movie * Agent Game * Agent Sport * Agent Hobby General vân vân.   Phần 3: Các thành phần trong prompt structure của một agents (to be input later)
```


có những trường nào về mặt kĩ thuật và trải nghiệm khi làm cái "Định nghĩa “thành công” cho từng loại Agent" lên reasoning để MECE và cho tôi các trường


prompt template có các trường như này, về việc tối ưu prompt tôi cũng cần bóc được các mảnh trong cấu trúc mọt cái prompt này có những cái gì, ngoài ra cũng muốn tạo ra nhiều phiên bản khác nhau dựa trên khung này để tìm được phương án tốt nhất cho từng agent nghiệp vụ. lên reasoning tôi nên làm như nào và cho tôi phương án